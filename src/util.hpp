#ifndef UTIL_HPP
#define UTIL_HPP
#include "vector.hpp"

struct LuaRandom {
    LuaRandom() {
        LuaRandom(0);
    }
    LuaRandom(double seed) {
        //Vector d(seed);
    }
};

#endif // UTIL_HPP